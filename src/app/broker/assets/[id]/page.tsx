import Link from "next/link";
import { ArrowLeft, CircleAlert, FileText, MapPin, Pencil } from "lucide-react";
import { BROKER_WORKSPACE } from "@/application/config/workspaceDemoData";
import { WorkspaceLayout } from "@/presentation/components/workspace/WorkspaceLayout";
import { StatusPill } from "@/presentation/components/workspace/StatusPill";

export default async function BrokerAssetDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const asset = BROKER_WORKSPACE.assets.find((item) => item.id === id); if (!asset) return <WorkspaceLayout kind="broker" title="Aset tidak ditemukan" description="Aset yang dicari tidak tersedia."><Link href="/broker/assets" className="text-primary hover:underline">Kembali ke aset saya</Link></WorkspaceLayout>; return <WorkspaceLayout kind="broker" title={asset.title} description={`${asset.id} · diperbarui ${asset.updatedAt}`}><Link href="/broker/assets" className="inline-flex items-center gap-2 text-label-md font-label-md text-primary hover:underline"><ArrowLeft className="h-4 w-4" />Kembali ke aset saya</Link><section className="mt-6 rounded-2xl border border-border-subtle bg-surface-container-lowest p-6 shadow-card"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-label-sm font-label-sm uppercase tracking-wider text-primary">Status pengajuan</p><div className="mt-3"><StatusPill status={asset.status} /></div></div><Link href="/broker/assets/new" className="inline-flex items-center gap-2 rounded-xl border border-primary px-4 py-2.5 text-label-md font-label-md text-primary"><Pencil className="h-4 w-4" />Edit aset</Link></div>{asset.issue && <div className="mt-6 flex gap-3 rounded-xl bg-orange-50 p-4 text-orange-900"><CircleAlert className="mt-0.5 h-5 w-5 shrink-0" /><div><h2 className="font-bold">Perbaikan diperlukan</h2><p className="mt-1 text-body-md leading-6">{asset.issue}</p><p className="mt-2 text-label-sm">Setelah diperbarui, ajukan kembali agar tim Kurata dapat melanjutkan review.</p></div></div>}<div className="mt-7 grid gap-5 sm:grid-cols-2"><Info icon={MapPin} label="Lokasi" value={asset.location} /><Info icon={FileText} label="Harga penawaran" value={asset.price} /></div></section></WorkspaceLayout>; }
function Info({ icon: Icon, label, value }: { icon: typeof MapPin; label: string; value: string }) { return <div className="flex gap-3"><Icon className="h-5 w-5 text-primary" /><div><p className="text-label-sm text-outline">{label}</p><p className="mt-1 text-label-md font-label-md text-on-surface">{value}</p></div></div>; }
