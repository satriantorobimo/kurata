import Link from "next/link";
import { ArrowRight, Plus, Search } from "lucide-react";
import { BROKER_WORKSPACE } from "@/application/config/workspaceDemoData";
import { WorkspaceLayout } from "@/presentation/components/workspace/WorkspaceLayout";
import { StatusPill } from "@/presentation/components/workspace/StatusPill";

export default function BrokerAssetsPage() { return <WorkspaceLayout kind="broker" title="Aset saya" description="Kelola draft, pengajuan, dan status verifikasi listing Anda."><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="relative max-w-sm flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-outline" /><input placeholder="Cari aset" className="w-full rounded-xl border border-border-subtle bg-surface-container-lowest py-2.5 pl-10 pr-4 text-body-md outline-none focus:border-primary" /></div><Link href="/broker/assets/new" className="inline-flex w-fit items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-label-md font-label-md text-on-primary"><Plus className="h-4 w-4" />Tambah aset</Link></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><Summary label="Sedang ditinjau" value={BROKER_WORKSPACE.assets.filter((item) => item.status === "under_review").length} /><Summary label="Perlu perbaikan" value={BROKER_WORKSPACE.assets.filter((item) => item.status === "changes_requested").length} /><Summary label="Sudah tayang" value={BROKER_WORKSPACE.assets.filter((item) => item.status === "published").length} /></div><section className="mt-6 overflow-hidden rounded-2xl border border-border-subtle bg-surface-container-lowest shadow-card"><div className="divide-y divide-border-subtle">{BROKER_WORKSPACE.assets.map((asset) => <Link key={asset.id} href={`/broker/assets/${asset.id}`} className="flex flex-col gap-3 p-5 transition-colors hover:bg-surface-container-low sm:flex-row sm:items-center sm:justify-between"><div><p className="text-label-sm text-outline">{asset.id} · diperbarui {asset.updatedAt}</p><h2 className="mt-1 text-label-md font-label-md text-on-surface">{asset.title}</h2><p className="mt-1 text-label-sm text-on-surface-variant">{asset.location} · {asset.price}</p>{asset.issue && <p className="mt-2 text-label-sm text-orange-800">{asset.issue}</p>}</div><div className="flex items-center gap-3"><StatusPill status={asset.status} /><ArrowRight className="h-4 w-4 text-primary" /></div></Link>)}</div></section><p className="mt-5 text-label-sm text-on-surface-variant">Status dan data aset masih memakai mode demo sampai penyimpanan aman dihubungkan.</p></WorkspaceLayout>; }
function Summary({ label, value }: { label: string; value: number }) { return <div className="rounded-xl bg-surface-container-low p-4"><p className="text-2xl font-bold text-on-surface">{value}</p><p className="mt-1 text-label-sm text-on-surface-variant">{label}</p></div>; }
